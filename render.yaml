services:
  - type: web
    name: die-history
    runtime: python
    rootDir: cloud
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SQL_PASS
        sync: false
